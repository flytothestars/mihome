export default (props) => ({
    offer: null,
    handleSubmit() {
        alert(1)
    },
    init() {
        let that = this;
        // that.$watch('searchObj', function (value) {
        //     console.log(value)
        // })
    }
})
